# Banking Platform

Monorepo for the real‑time digital banking microservices.

## Getting started

1. Clone the repo
2. Run `docker-compose -f infrastructure/docker/docker-compose.yml up`
3. Start any service with `mvn spring-boot:run`

## In Progress